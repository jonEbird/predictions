; This is a public open-source project: always resolve packages from the public
; npm registry, never from an internal mirror. A project-level .npmrc overrides
; any registry set in ~/.npmrc, which keeps internal URLs out of package-lock.json
; for contributors whose global npm config points elsewhere.
registry=https://registry.npmjs.org/
